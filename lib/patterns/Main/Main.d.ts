import { ReactNode } from 'react';
export declare type MainProps = {
    children: ReactNode;
};
export declare const Main: ({ children, ...props }: MainProps) => import("theme-ui/jsx-runtime").JSX.Element;
